# LangChain Sample Agent

This sample shows a minimal LangChain-based agent with tool calling.

## Run

```bash
cd agents/course/langchain
python main.py
```

Or with a custom prompt:

```bash
python main.py --prompt "What is 9 + 14?"
```

## Requirements

Set `OPENAI_API_KEY` in your environment before running.
